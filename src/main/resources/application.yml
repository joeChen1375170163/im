#自定义参数imconf:   exchange: IMExchange  agentQueue: agent_queue  agentKey: im.agent.key  customerQueue: customer_queue  customerKey: im.customer.key spring:  # 多环境配置  profiles:    active: dev  application:
    name: im  #视图解析器  mvc:    view:      prefix: classpath:/templates/      suffix: .html
   #控制台日志颜色    
  output:    ansi:      enabled: detect       #Freemarker 配置  freemarker:    template-loader-path: classpath:/templates/    cache: false    charset: UTF-8    check-template-location: true    content-type: text/html    expose-request-attributes: true    expose-session-attributes: true    request-context-attribute: request    suffix: .html  
  jpa:    hibernate:      ddl-auto: none      naming:        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl #无修改命名策略    show-sql: false    properties:      hibernate:         enable_lazy_load_no_trans: true    #jackson 配置    jackson:    time-zone: GMT+8  #日期类型使用中国时区    date-format: yyyy-MM-dd HH:mm:ss #日期格式化    #default-property-inclusion: non-empty #设置空如何序列化    serialization:      indent-output: false #格式化输出      fail-on-empty-beans: false  #忽略无法转换的对象    deserialization:      fail-on-unknown-properties: false #允许对象忽略json中不存在的属性    parser:      allow-unquoted-control-chars: true  #允许出现特殊字符和转义符      allow-single-quotes: true #允许出现单引号    #文件大小配置  servlet:    multipart:      enabled: true      max-file-size: 100MB      max-request-size: 100MB               
